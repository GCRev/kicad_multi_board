# Fixtures and golden files are compared byte-for-byte; never let git rewrite their line endings.
tests/fixtures/** -text
tests/golden/** -text

# Shell scripts must keep LF endings or sh fails on a CRLF checkout.
*.sh text eol=lf
